{
  "name": "mysuru-digital-twin-integrated",
  "version": "1.0.0",
  "scripts": {
    "start": "cd server && node index.js",
    "dev": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
